<? foreach ($this->mapper()->simplify($this->driver->getFormats()) as $format): ?>
  <span class="label label-info <?=$this->record($this->driver)->getFormatClass($format)?>"><?=$this->transEsc($format)?></span>
<? endforeach; ?>
